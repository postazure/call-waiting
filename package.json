{
  "name": "call-waiting",
  "version": "1.0.0",
  "description": "Allows a user to initialize a method call with arguments to be called at a later date.. The method can continue to have arguments added over time until it is invoked.",
  "main": "./lib/call-waiting.js",
  "scripts": {
    "test": "npm run compile && mocha --compilers js:babel-core/register",
    "compile": "babel -d lib/ src/"
  },
  "author": "Postazure",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.25.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1",
    "chai": "^4.1.0",
    "mocha": "^3.4.2",
    "sinon": "^2.3.8",
    "sinon-chai": "^2.11.0"
  }
}
